export function multiplyNumber(num1, num2) {
    return num1 * num2
};

export function devideNumber(num1, num2) {
    return num1 / num2
};

export function generateRandomArrayIndex(length) {
    return Math.floor(Math.random() * length);
}