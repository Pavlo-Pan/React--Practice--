
import styles from "./Footer.module.css";

const Footer = () =>{
    return (
        <footer>
            <button className={styles.btn} >futter button</button>
        </footer>
    )
};

export default Footer;